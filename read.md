# 基于多智能体的buggy detection测试用例的生成
## 任务描述
defect4j数据集中，提供了bug版本和fixed版本的代码，针对bug版本中的被测函数，使用llm对其自动化生成测试用例，如果测试用例在bug版本上未通过，但在fixed版本上通过，则认为该测试用例为buggy detection的测试用例。但该任务具有复杂性，当测试用例执行不通过时，需要判断测试用例生成错误还是检测到代码错误的复杂情况，所以使用多智能体的方式完成任务。

## 当前方案
第一阶段根据数据集中的project生成全局代码知识库，构建文件级、类级、方法级等不同粒度的对象之间的关联
### 智能体设计
1.requirement agent：
    第一阶段：自主规划并调用工具收集函数，收集生成高质量test requirement所需要的代码上下文。
    第二阶段：根据第一阶段收集到的代码上下文以及被测函数的javadoc文档生成高质量test requirement以指导后续的测试用例生成。
    第三阶段：通过跟reviewer agent交互，并根据reviewer提出的修改意见不断提高requirement的质量。
2.reviewer agent
    任务1：与requirement agent交互，评判其生成的requirement是否客观、完整、正确。如果不符合要求，返回修改意见，如果符合要求，通过批准。
    任务2：与test agent交互，当test agent生成的测试用例执行未通过时，根据报错信息以及相关知识、记忆等能力做出判断是测试用例出错还是成功触发了代码中的bug。如果测试用例生成错误，提供修改意见，如果触发代码bug，是否接受测试用例。并且根据测试用例的覆盖率信息以及代码知识库中的知识，为test agent提高测试覆盖率提出建议。
3.test agent
    根据requirement agent生成的test requirement生成测试用例，尽可能覆盖更多的分支以及触发bug。将生成测试用例以及执行结果与reviewer agent进行交互，提高测试用例的准确度、覆盖率等指标。

### 整体流程设计
对于数据集中的每一个project，进行代码分析，构建代码知识库。针对被测函数，通过requirement agent对其进行test requirement生成并与reviewer交互提高质量。test agent根据生成的test requirement生成测试用例，并尝试执行，根据执行结果与reviewer交互直到最大迭代次数或覆盖率无法提高。

### 实验评测方式
对于生成的测试文件，在bug版本与fixed版本上分别进行测试，如果在bug版本上的测试未通过并且在fixed版本的测试通过，则视为有效test，统计符合要求的test数量。对数据集中的每一个被测函数进行统计后，得到总共的有效test数量与比例。

## 需要优化的技术点
1.智能体分工以及任务设计是否合理，针对这个任务情景给出意见
2.针对这个复杂情景，当测试用例执行未通过，可能有测试用例生成错误和测试用例成功检测出bug等不同原因，如何更好的让reviewer agent做出正确的判断以完成更好的任务表现。
3.对于agent中的planning方式，memory机制以及智能体交互中的上下文构建方式等多智能体技术中的常见技术细节，给出实现方式
4.如何避免多智能体交互中的错误累积所带来的影响
5.memory机制设计时，对于每个智能体，什么内容应该加入到记忆中？怎么保证加入内容的正确性？如何评判这些内容加入记忆的有效性？
6.可能还有其它未考虑到的技术问题